#!/bin/sh
set -eu

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if command -v systemctl >/dev/null 2>&1; then
        systemctl stop pepersprayd.service >/dev/null 2>&1 || true
        systemctl disable pepersprayd.service >/dev/null 2>&1 || true
    fi
fi

exit 0
